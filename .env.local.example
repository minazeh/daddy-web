# Copy this file to .env.local and fill in the value.
# Server-side only — this never reaches the browser.
#
# MongoDB Atlas connection string for the `discordbot` database.
# The web app READS the `members` collection (kept fresh by the Discord bot)
# and READS/WRITES the `parties` collection (owned by this app).
MONGODB_URI=
