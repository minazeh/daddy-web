"use client";

// DEPRECATED: the two-pane full-width builder replaced the old single-section
// PartyBoard. This file is kept (not deleted) and re-exports the current shell
// so any lingering import resolves. New code should import BuilderShell.
export { BuilderShell as PartyBoard } from "./BuilderShell";
